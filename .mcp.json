{
  "mcpServers": {
    "neon": {
      "command": "bash",
      "args": ["-c", "set -a; source /Users/playra/vibee/gleam/.env; set +a; exec npx -y @neondatabase/mcp-server-neon start $NEON_API_KEY"]
    }
  }
}
