name: serve_vllm_v1500
version: "15.0.0"
language: zig
module: serve_vllm
types:
  Serve_vllmConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Serve_vllmState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Serve_vllmResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_serve_vllm
    given: Config
    when: Init
    then: State
