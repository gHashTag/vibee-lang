name: embedding_voyage_v1089
version: "10.8.9"
language: zig
module: embedding_voyage
types:
  Embedding_voyageConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Embedding_voyageState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Embedding_voyageResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_embedding_voyage
    given: Config
    when: Init
    then: State
  - name: process_embedding_voyage
    given: State
    when: Process
    then: Result
