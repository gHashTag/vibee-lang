name: embedding_bge_v1093
version: "10.9.3"
language: zig
module: embedding_bge
types:
  Embedding_bgeConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Embedding_bgeState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Embedding_bgeResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_embedding_bge
    given: Config
    when: Init
    then: State
  - name: process_embedding_bge
    given: State
    when: Process
    then: Result
