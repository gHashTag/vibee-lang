name: safety_v798
version: "7.9.8"
language: zig
module: safety
types:
  SafetyConfig: { fields: { id: String, enabled: Bool, params: Object } }
  SafetyState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  SafetyResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_safety
    given: Config
    when: Init
    then: State
  - name: process_safety
    given: State
    when: Process
    then: Result
