name: logging_v801
version: "8.0.1"
language: zig
module: logging
types:
  LoggingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  LoggingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  LoggingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_logging
    given: Config
    when: Init
    then: State
  - name: process_logging
    given: State
    when: Process
    then: Result
