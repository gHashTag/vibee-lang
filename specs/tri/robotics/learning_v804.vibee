name: learning_v804
version: "8.0.4"
language: zig
module: learning
types:
  LearningConfig: { fields: { id: String, enabled: Bool, params: Object } }
  LearningState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  LearningResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_learning
    given: Config
    when: Init
    then: State
  - name: process_learning
    given: State
    when: Process
    then: Result
