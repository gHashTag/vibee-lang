name: continual_replay_v1409
version: "14.0.9"
language: zig
module: continual_replay
types:
  Continual_replayConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Continual_replayState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Continual_replayResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_continual_replay
    given: Config
    when: Init
    then: State
