name: attention_rollout_v1629
version: "16.2.9"
language: zig
module: attention_rollout
types:
  Attention_rolloutConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Attention_rolloutState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Attention_rolloutResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_attention_rollout
    given: Config
    when: Init
    then: State
