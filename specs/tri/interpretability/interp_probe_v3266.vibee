name: interp_probe_v3266
version: "1.0.0"
language: zig
module: interp_probe_v3266
domain: interpretability

types:
  Interp_probeConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Interp_probeState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Interp_probeResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_interp_probe
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_interp_probe
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_interp_probe
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_interp_probe
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
