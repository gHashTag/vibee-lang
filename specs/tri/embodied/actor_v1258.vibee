name: actor_v1258
version: "12.5.8"
language: zig
module: actor
types:
  ActorConfig: { fields: { id: String, enabled: Bool, params: Object } }
  ActorState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  ActorResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_actor
    given: Config
    when: Init
    then: State
