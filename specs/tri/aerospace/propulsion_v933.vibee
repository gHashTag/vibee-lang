name: propulsion_v933
version: "9.3.3"
language: zig
module: propulsion
types:
  PropulsionConfig: { fields: { id: String, enabled: Bool, params: Object } }
  PropulsionState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  PropulsionResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_propulsion
    given: Config
    when: Init
    then: State
  - name: process_propulsion
    given: State
    when: Process
    then: Result
