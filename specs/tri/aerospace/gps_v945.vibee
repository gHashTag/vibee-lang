name: gps_v945
version: "9.4.5"
language: zig
module: gps
types:
  GpsConfig: { fields: { id: String, enabled: Bool, params: Object } }
  GpsState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  GpsResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_gps
    given: Config
    when: Init
    then: State
  - name: process_gps
    given: State
    when: Process
    then: Result
