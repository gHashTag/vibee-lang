name: tracking_v942
version: "9.4.2"
language: zig
module: tracking
types:
  TrackingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  TrackingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  TrackingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_tracking
    given: Config
    when: Init
    then: State
  - name: process_tracking
    given: State
    when: Process
    then: Result
