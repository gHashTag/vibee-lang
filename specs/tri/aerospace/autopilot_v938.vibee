name: autopilot_v938
version: "9.3.8"
language: zig
module: autopilot
types:
  AutopilotConfig: { fields: { id: String, enabled: Bool, params: Object } }
  AutopilotState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  AutopilotResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_autopilot
    given: Config
    when: Init
    then: State
  - name: process_autopilot
    given: State
    when: Process
    then: Result
