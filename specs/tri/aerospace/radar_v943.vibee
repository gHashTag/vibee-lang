name: radar_v943
version: "9.4.3"
language: zig
module: radar
types:
  RadarConfig: { fields: { id: String, enabled: Bool, params: Object } }
  RadarState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  RadarResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_radar
    given: Config
    when: Init
    then: State
  - name: process_radar
    given: State
    when: Process
    then: Result
