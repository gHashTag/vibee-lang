name: diagnostics_v958
version: "9.5.8"
language: zig
module: diagnostics
types:
  DiagnosticsConfig: { fields: { id: String, enabled: Bool, params: Object } }
  DiagnosticsState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  DiagnosticsResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_diagnostics
    given: Config
    when: Init
    then: State
  - name: process_diagnostics
    given: State
    when: Process
    then: Result
