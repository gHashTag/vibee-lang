name: trim_v940
version: "9.4.0"
language: zig
module: trim
types:
  TrimConfig: { fields: { id: String, enabled: Bool, params: Object } }
  TrimState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  TrimResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_trim
    given: Config
    when: Init
    then: State
  - name: process_trim
    given: State
    when: Process
    then: Result
