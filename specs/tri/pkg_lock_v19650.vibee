name: pkg_lock_v19650
version: "1.0.0"
language: zig
module: pkg_lock_v19650

types:
  LockFile:
    fields:
      version: Int
      packages: Map<String,String>
      checksums: Map<String,String>

  LockEntry:
    fields:
      name: String
      version: String
      resolved: String
      integrity: String

behaviors:
  - name: lock_read
    given: "Lock file path"
    when: "Manager reads lock"
    then: "Returns lock data"

  - name: lock_write
    given: "Resolution graph"
    when: "Manager writes lock"
    then: "Creates lock file"

  - name: lock_verify
    given: "Lock file"
    when: "Manager verifies"
    then: "Returns verification result"
