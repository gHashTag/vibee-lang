name: self_critique_v2749
version: "1.0.0"
language: zig
module: self_critique_v2749
domain: self_improve

types:
  Self_critiqueConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Self_critiqueState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Self_critiqueResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_self_critique
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_self_critique
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_self_critique
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_self_critique
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
