name: planet_drill_v3686
version: "1.0.0"
language: zig
module: planet_drill_v3686
domain: planetary_exploration

types:
  Planet_drillConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Planet_drillState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Planet_drillResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_planet_drill
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_planet_drill
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_planet_drill
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_planet_drill
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
