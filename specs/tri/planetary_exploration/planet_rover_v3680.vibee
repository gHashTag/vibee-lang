name: planet_rover_v3680
version: "1.0.0"
language: zig
module: planet_rover_v3680
domain: planetary_exploration

types:
  Planet_roverConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Planet_roverState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Planet_roverResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_planet_rover
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_planet_rover
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_planet_rover
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_planet_rover
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
