name: planet_probe_v3683
version: "1.0.0"
language: zig
module: planet_probe_v3683
domain: planetary_exploration

types:
  Planet_probeConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Planet_probeState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Planet_probeResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_planet_probe
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_planet_probe
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_planet_probe
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_planet_probe
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
