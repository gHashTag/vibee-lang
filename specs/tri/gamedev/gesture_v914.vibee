name: gesture_v914
version: "9.1.4"
language: zig
module: gesture
types:
  GestureConfig: { fields: { id: String, enabled: Bool, params: Object } }
  GestureState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  GestureResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_gesture
    given: Config
    when: Init
    then: State
  - name: process_gesture
    given: State
    when: Process
    then: Result
