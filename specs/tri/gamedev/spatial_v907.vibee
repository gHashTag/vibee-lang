name: spatial_v907
version: "9.0.7"
language: zig
module: spatial
types:
  SpatialConfig: { fields: { id: String, enabled: Bool, params: Object } }
  SpatialState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  SpatialResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_spatial
    given: Config
    when: Init
    then: State
  - name: process_spatial
    given: State
    when: Process
    then: Result
