name: world_reward_v2654
version: "1.0.0"
language: zig
module: world_reward_v2654
domain: world_models

types:
  World_rewardConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  World_rewardState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  World_rewardResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_world_reward
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_world_reward
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_world_reward
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_world_reward
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
