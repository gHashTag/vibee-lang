name: test_benchmark_v19870
version: "1.0.0"
language: zig
module: test_benchmark_v19870

types:
  Benchmark:
    fields:
      name: String
      iterations: Int
      warmup: Int

  BenchmarkResult:
    fields:
      mean_ns: Int
      std_dev_ns: Int
      min_ns: Int
      max_ns: Int
      throughput: Float

behaviors:
  - name: benchmark_run
    given: "Benchmark definition"
    when: "Runner runs"
    then: "Returns benchmark result"

  - name: benchmark_compare
    given: "Two benchmarks"
    when: "Comparator compares"
    then: "Returns comparison"

  - name: benchmark_report
    given: "Benchmark results"
    when: "Reporter reports"
    then: "Generates benchmark report"
