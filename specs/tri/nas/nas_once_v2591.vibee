name: nas_once_v2591
version: "1.0.0"
language: zig
module: nas_once_v2591
domain: nas

types:
  Nas_onceConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Nas_onceState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Nas_onceResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_nas_once
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_nas_once
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_nas_once
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_nas_once
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
