name: event_perception_v17470
version: "1.0.0"
language: zig
module: event_perception

types:
  Event:
    fields:
      event_type: String
      participants: String
      temporal: String

  EventSequence:
    fields:
      events: String
      causal_links: String

behaviors:
  - name: detect_events
    given: Video
    when: Event detection
    then: Return events

  - name: segment_activity
    given: Video
    when: Activity segmentation
    then: Return segments
