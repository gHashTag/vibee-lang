name: mcp_sampling_v1065
version: "10.6.5"
language: zig
module: mcp_sampling
types:
  Mcp_samplingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Mcp_samplingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Mcp_samplingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_mcp_sampling
    given: Config
    when: Init
    then: State
  - name: process_mcp_sampling
    given: State
    when: Process
    then: Result
