name: mcp_transport_v1061
version: "10.6.1"
language: zig
module: mcp_transport
types:
  Mcp_transportConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Mcp_transportState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Mcp_transportResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_mcp_transport
    given: Config
    when: Init
    then: State
  - name: process_mcp_transport
    given: State
    when: Process
    then: Result
