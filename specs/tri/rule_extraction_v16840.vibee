name: rule_extraction_v16840
version: "1.0.0"
language: zig
module: rule_extraction

types:
  ExtractedRule:
    fields:
      conditions: String
      conclusion: String
      fidelity: Float
      coverage: Float

  RuleSet:
    fields:
      rules: String
      default_class: String

  ExtractionMethod:
    fields:
      method: String
      complexity_penalty: Float

  ExtractionResult:
    fields:
      rules: String
      global_fidelity: Float
      interpretability: Float

behaviors:
  - name: extract_rules
    given: Model and data
    when: Rule extraction algorithm
    then: Return rule set

  - name: prune_rules
    given: Rule set
    when: Complexity reduction
    then: Return pruned rules

  - name: evaluate_fidelity
    given: Rules and model
    when: Compare predictions
    then: Return fidelity score
