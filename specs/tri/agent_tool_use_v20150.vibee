name: agent_tool_use_v20150
version: "1.0.0"
language: zig
module: agent_tool_use_v20150

types:
  Tool:
    fields:
      name: String
      description: String
      parameters: Map<String,String>

  ToolResult:
    fields:
      success: Bool
      output: String
      side_effects: List<String>

behaviors:
  - name: tool_select
    given: "Task description"
    when: "Agent selects tool"
    then: "Returns best tool"

  - name: tool_execute
    given: "Tool and parameters"
    when: "Agent executes"
    then: "Returns tool output"

  - name: tool_compose
    given: "Multiple tools"
    when: "Agent chains tools"
    then: "Returns composed result"
