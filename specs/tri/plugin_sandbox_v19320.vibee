name: plugin_sandbox_v19320
version: "1.0.0"
language: zig
module: plugin_sandbox_v19320

types:
  Sandbox:
    fields:
      id: String
      permissions: List<String>
      memory_limit: Int
      timeout_ms: Int

  SandboxResult:
    fields:
      success: Bool
      output: String
      memory_used: Int
      time_used: Int

behaviors:
  - name: sandbox_create
    given: "Sandbox config"
    when: "Sandbox creates"
    then: "Returns sandbox instance"

  - name: sandbox_execute
    given: "Plugin code"
    when: "Sandbox executes"
    then: "Returns sandboxed result"

  - name: sandbox_destroy
    given: "Sandbox instance"
    when: "Sandbox destroys"
    then: "Cleans up resources"
