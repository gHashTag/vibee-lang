name: automl_hpo_v2560
version: "1.0.0"
language: zig
module: automl_hpo_v2560
domain: automl

types:
  Automl_hpoConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Automl_hpoState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Automl_hpoResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_automl_hpo
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_automl_hpo
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_automl_hpo
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_automl_hpo
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
