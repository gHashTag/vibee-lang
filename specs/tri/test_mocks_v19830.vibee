name: test_mocks_v19830
version: "1.0.0"
language: zig
module: test_mocks_v19830

types:
  Mock:
    fields:
      name: String
      interface: String
      expectations: List<String>

  MockCall:
    fields:
      method: String
      args: List<String>
      return_value: String

behaviors:
  - name: mock_create
    given: "Interface definition"
    when: "Mocker creates"
    then: "Returns mock object"

  - name: mock_expect
    given: "Expected call"
    when: "Mocker expects"
    then: "Records expectation"

  - name: mock_verify
    given: "Mock object"
    when: "Mocker verifies"
    then: "Checks expectations met"
