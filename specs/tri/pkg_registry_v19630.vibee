name: pkg_registry_v19630
version: "1.0.0"
language: zig
module: pkg_registry_v19630

types:
  Registry:
    fields:
      url: String
      auth_token: String
      cache_dir: String

  RegistryPackage:
    fields:
      name: String
      versions: List<String>
      latest: String
      downloads: Int

behaviors:
  - name: registry_search
    given: "Search query"
    when: "Registry searches"
    then: "Returns matching packages"

  - name: registry_fetch
    given: "Package spec"
    when: "Registry fetches"
    then: "Downloads package"

  - name: registry_publish
    given: "Package tarball"
    when: "Registry publishes"
    then: "Publishes package"
