name: cli_eval_v19150
version: "1.0.0"
language: zig
module: cli_eval_v19150

types:
  EvalContext:
    fields:
      expression: String
      variables: Map<String,String>
      mode: String

  EvalResult:
    fields:
      value: String
      type_info: String
      ternary_state: String
      phi_value: Float

behaviors:
  - name: eval_ternary
    given: "Ternary expression"
    when: "Evaluator processes"
    then: "Returns ternary result"

  - name: eval_phi
    given: "Phi expression"
    when: "Evaluator calculates"
    then: "Returns golden ratio value"

  - name: eval_expression
    given: "General expression"
    when: "Evaluator processes"
    then: "Returns computed value"
