name: build_core_v19700
version: "1.0.0"
language: zig
module: build_core_v19700

types:
  BuildConfig:
    fields:
      target: String
      mode: String
      output_dir: String
      sources: List<String>

  BuildResult:
    fields:
      success: Bool
      artifacts: List<String>
      duration_ms: Int
      warnings: Int

behaviors:
  - name: build_project
    given: "Build configuration"
    when: "Builder builds"
    then: "Returns build result"

  - name: build_clean
    given: "Clean request"
    when: "Builder cleans"
    then: "Removes build artifacts"

  - name: build_rebuild
    given: "Rebuild request"
    when: "Builder rebuilds"
    then: "Full rebuild from scratch"
