name: arch_v2296
version: "1.0.0"
language: zig
module: arch_v2296

types:
  Config2296:
    fields:
      hidden_dim: Int
      num_layers: Int
      num_heads: Int
      vocab_size: Int

  State2296:
    fields:
      weights: String
      activations: String
      gradients: String

behaviors:
  - name: forward
    given: "Input tensor"
    when: "Forward pass"
    then: "Output computed"

  - name: backward
    given: "Loss gradient"
    when: "Backward pass"
    then: "Gradients computed"

  - name: optimize
    given: "Current state"
    when: "Optimization step"
    then: "Weights updated"
