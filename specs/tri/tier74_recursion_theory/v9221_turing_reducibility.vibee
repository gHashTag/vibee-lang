# VIBEE v9221: Turing Reducibility
name: turing_reducibility
version: "9221.0.0"
language: zig
module: turing_reducibility

types:
  RecursionConcept:
    fields:
      name: String
      definition: String
      properties: List<String>
      complexity: String

  TuringDegree:
    fields:
      representative: String
      jump: String
      below: List<String>
      above: List<String>

  HierarchyLevel:
    fields:
      sigma: Int
      pi: Int
      delta: Int
      complete_set: String

behaviors:
  - name: compute_degree
    given: "Set specification"
    when: "Finding Turing degree"
    then: "Returns degree"

  - name: classify_hierarchy
    given: "Set"
    when: "Determining level"
    then: "Returns hierarchy classification"
