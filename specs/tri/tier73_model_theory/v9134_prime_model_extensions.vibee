# VIBEE v9134: Prime Model Extensions
name: prime_model_extensions
version: "9134.0.0"
language: zig
module: prime_model_extensions

types:
  StabilityProperty:
    fields:
      theory: String
      stable: Bool
      superstable: Bool
      omega_stable: Bool

  ForkingRelation:
    fields:
      type_p: String
      set_a: String
      set_b: String
      forks: Bool

  RankFunction:
    fields:
      type: String
      morley_rank: Int
      u_rank: Int
      lascar_rank: Int

behaviors:
  - name: compute_rank
    given: "Type over set"
    when: "Calculating rank"
    then: "Returns rank value"

  - name: test_forking
    given: "Type and sets"
    when: "Checking independence"
    then: "Returns forking status"
