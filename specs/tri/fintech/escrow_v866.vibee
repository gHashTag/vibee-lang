name: escrow_v866
version: "8.6.6"
language: zig
module: escrow
types:
  EscrowConfig: { fields: { id: String, enabled: Bool, params: Object } }
  EscrowState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  EscrowResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_escrow
    given: Config
    when: Init
    then: State
  - name: process_escrow
    given: State
    when: Process
    then: Result
