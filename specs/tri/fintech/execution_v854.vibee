name: execution_v854
version: "8.5.4"
language: zig
module: execution
types:
  ExecutionConfig: { fields: { id: String, enabled: Bool, params: Object } }
  ExecutionState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  ExecutionResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_execution
    given: Config
    when: Init
    then: State
  - name: process_execution
    given: State
    when: Process
    then: Result
