name: streaming_v885
version: "8.8.5"
language: zig
module: streaming
types:
  StreamingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  StreamingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  StreamingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_streaming
    given: Config
    when: Init
    then: State
  - name: process_streaming
    given: State
    when: Process
    then: Result
