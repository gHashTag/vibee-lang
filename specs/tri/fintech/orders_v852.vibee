name: orders_v852
version: "8.5.2"
language: zig
module: orders
types:
  OrdersConfig: { fields: { id: String, enabled: Bool, params: Object } }
  OrdersState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  OrdersResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_orders
    given: Config
    when: Init
    then: State
  - name: process_orders
    given: State
    when: Process
    then: Result
