name: remittance_v865
version: "8.6.5"
language: zig
module: remittance
types:
  RemittanceConfig: { fields: { id: String, enabled: Bool, params: Object } }
  RemittanceState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  RemittanceResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_remittance
    given: Config
    when: Init
    then: State
  - name: process_remittance
    given: State
    when: Process
    then: Result
