name: fraud_v877
version: "8.7.7"
language: zig
module: fraud
types:
  FraudConfig: { fields: { id: String, enabled: Bool, params: Object } }
  FraudState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  FraudResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_fraud
    given: Config
    when: Init
    then: State
  - name: process_fraud
    given: State
    when: Process
    then: Result
