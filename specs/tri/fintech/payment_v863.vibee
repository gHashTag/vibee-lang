name: payment_v863
version: "8.6.3"
language: zig
module: payment
types:
  PaymentConfig: { fields: { id: String, enabled: Bool, params: Object } }
  PaymentState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  PaymentResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_payment
    given: Config
    when: Init
    then: State
  - name: process_payment
    given: State
    when: Process
    then: Result
