name: alignment_v817
version: "8.1.7"
language: zig
module: alignment
types:
  AlignmentConfig: { fields: { id: String, enabled: Bool, params: Object } }
  AlignmentState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  AlignmentResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_alignment
    given: Config
    when: Init
    then: State
  - name: process_alignment
    given: State
    when: Process
    then: Result
