name: folding_v827
version: "8.2.7"
language: zig
module: folding
types:
  FoldingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  FoldingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  FoldingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_folding
    given: Config
    when: Init
    then: State
  - name: process_folding
    given: State
    when: Process
    then: Result
