name: discovery_v833
version: "8.3.3"
language: zig
module: discovery
types:
  DiscoveryConfig: { fields: { id: String, enabled: Bool, params: Object } }
  DiscoveryState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  DiscoveryResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_discovery
    given: Config
    when: Init
    then: State
  - name: process_discovery
    given: State
    when: Process
    then: Result
