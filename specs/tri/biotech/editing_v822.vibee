name: editing_v822
version: "8.2.2"
language: zig
module: editing
types:
  EditingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  EditingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  EditingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_editing
    given: Config
    when: Init
    then: State
  - name: process_editing
    given: State
    when: Process
    then: Result
