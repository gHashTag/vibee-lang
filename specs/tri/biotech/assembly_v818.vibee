name: assembly_v818
version: "8.1.8"
language: zig
module: assembly
types:
  AssemblyConfig: { fields: { id: String, enabled: Bool, params: Object } }
  AssemblyState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  AssemblyResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_assembly
    given: Config
    when: Init
    then: State
  - name: process_assembly
    given: State
    when: Process
    then: Result
