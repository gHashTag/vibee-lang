name: docking_v828
version: "8.2.8"
language: zig
module: docking
types:
  DockingConfig: { fields: { id: String, enabled: Bool, params: Object } }
  DockingState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  DockingResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_docking
    given: Config
    when: Init
    then: State
  - name: process_docking
    given: State
    when: Process
    then: Result
