name: alignment_dpo_v1120
version: "11.2.0"
language: zig
module: alignment_dpo
types:
  Alignment_dpoConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Alignment_dpoState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Alignment_dpoResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_alignment_dpo
    given: Config
    when: Init
    then: State
  - name: process_alignment_dpo
    given: State
    when: Process
    then: Result
