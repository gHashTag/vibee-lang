name: integration_layer_v20840
version: "1.0.0"
language: zig
module: integration_layer_v20840

types:
  Integration:
    fields:
      name: String
      protocol: String
      endpoint: String

  IntegrationResult:
    fields:
      connected: Bool
      latency_ms: Int
      data: String

behaviors:
  - name: integration_connect
    given: "Endpoint config"
    when: "Connector connects"
    then: "Returns connection"

  - name: integration_send
    given: "Message"
    when: "Sender sends"
    then: "Returns response"

  - name: integration_receive
    given: "Connection"
    when: "Receiver receives"
    then: "Returns message"
