name: ts_dlinear_v2978
version: "1.0.0"
language: zig
module: ts_dlinear_v2978
domain: time_series

types:
  Ts_dlinearConfig:
    fields:
      enabled: Bool
      version: String
      params: Object

  Ts_dlinearState:
    fields:
      initialized: Bool
      data: Object
      timestamp: Timestamp

  Ts_dlinearResult:
    fields:
      success: Bool
      output: Object
      metrics: Object

behaviors:
  - name: init_ts_dlinear
    given: "Config provided"
    when: "Initialize called"
    then: "State created"

  - name: process_ts_dlinear
    given: "Valid state"
    when: "Process invoked"
    then: "Result returned"

  - name: validate_ts_dlinear
    given: "Input data"
    when: "Validation requested"
    then: "Validation result"

  - name: optimize_ts_dlinear
    given: "Current state"
    when: "Optimization triggered"
    then: "Optimized state"
