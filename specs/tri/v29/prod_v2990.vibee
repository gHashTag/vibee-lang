name: prod_v2990
version: "1.0.0"
language: zig
module: prod_v2990

types:
  ProdConfig:
    fields:
      replicas: Int
      timeout_ms: Int

behaviors:
  - name: deploy
    given: "Model artifact"
    when: "Deployment"
    then: "Service running"
