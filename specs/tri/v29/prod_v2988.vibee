name: prod_v2988
version: "1.0.0"
language: zig
module: prod_v2988

types:
  ProdConfig:
    fields:
      replicas: Int
      timeout_ms: Int

behaviors:
  - name: deploy
    given: "Model artifact"
    when: "Deployment"
    then: "Service running"
