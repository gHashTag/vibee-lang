name: quality_pandera_v1571
version: "15.7.1"
language: zig
module: quality_pandera
types:
  Quality_panderaConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Quality_panderaState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Quality_panderaResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_quality_pandera
    given: Config
    when: Init
    then: State
