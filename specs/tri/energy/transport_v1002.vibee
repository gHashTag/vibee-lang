name: transport_v1002
version: "10.0.2"
language: zig
module: transport
types:
  TransportConfig: { fields: { id: String, enabled: Bool, params: Object } }
  TransportState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  TransportResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_transport
    given: Config
    when: Init
    then: State
  - name: process_transport
    given: State
    when: Process
    then: Result
