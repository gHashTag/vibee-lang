name: battery_v981
version: "9.8.1"
language: zig
module: battery
types:
  BatteryConfig: { fields: { id: String, enabled: Bool, params: Object } }
  BatteryState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  BatteryResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_battery
    given: Config
    when: Init
    then: State
  - name: process_battery
    given: State
    when: Process
    then: Result
