name: lsp_hover_v19430
version: "1.0.0"
language: zig
module: lsp_hover_v19430

types:
  HoverInfo:
    fields:
      contents: String
      range_start: Int
      range_end: Int

  HoverRequest:
    fields:
      uri: String
      line: Int
      character: Int

behaviors:
  - name: hover_provide
    given: "Hover request"
    when: "Provider provides"
    then: "Returns hover info"

  - name: hover_format
    given: "Symbol info"
    when: "Formatter formats"
    then: "Returns markdown content"

  - name: hover_lookup
    given: "Position"
    when: "Lookup finds symbol"
    then: "Returns symbol info"
