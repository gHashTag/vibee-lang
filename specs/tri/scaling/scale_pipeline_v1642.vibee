name: scale_pipeline_v1642
version: "16.4.2"
language: zig
module: scale_pipeline
types:
  Scale_pipelineConfig: { fields: { id: String, enabled: Bool, params: Object } }
  Scale_pipelineState: { fields: { status: String, data: Object, timestamp: Timestamp } }
  Scale_pipelineResult: { fields: { success: Bool, output: Object, error: Option<String> } }
behaviors:
  - name: init_scale_pipeline
    given: Config
    when: Init
    then: State
