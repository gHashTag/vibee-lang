name: recovery_v18660
version: "1.0.0"
language: zig
module: recovery

types:
  RecoveryPlan:
    fields:
      scenario: String
      steps: String
      resources: String
  RecoveryOutcome:
    fields:
      success: Bool
      timeline: String

behaviors:
  - name: plan_recovery
    given: Catastrophe
    when: Planning
    then: Return recovery plan
  - name: execute_recovery
    given: Plan
    when: Execution
    then: Return recovery outcome
