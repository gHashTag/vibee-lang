# 🔥 VIBEE FORMAT RULES - DO NOT FORGET!

## ⚠️ CRITICAL: Given/When/Then is MANDATORY!

**NEVER FORGET THIS:**

1. ✅ **YAML syntax** - Standard format
2. ✅ **Given/When/Then** - Behavior-Driven Development (BDD)
3. ✅ **test_cases** - Multiple test scenarios per behavior

## ✅ CORRECT Format:

```yaml
name: module_name
version: "1.0.0"
language: gleam
module: module_name

behaviors:
  - name: behavior_name
    given: Initial condition
    when: Action performed
    then: Expected result
    test_cases:
      - name: test_case_1
        input: {param1: value1}
        expected: {result: value}
      - name: test_case_2
        input: {param1: value2}
        expected: {error: "message"}

types:
  TypeName:
    field1: type1
    field2: type2

functions:
  - name: function_name
    params: {param1: type1}
    returns: ReturnType
```

## ❌ WRONG Format:

```yaml
# NO Given/When/Then - WRONG!
functions:
  - name: function_name
    params: [...]
```

## 📚 Real Examples:

Look at `honeycomb/*/spec.yml` files:
- `honeycomb/telegram/telegram_bot/spec.yml` ✅
- `honeycomb/knowledge/knowledge_base/spec.yml` ✅
- `honeycomb/tools/language_generator/spec.yml` ✅

## 🎯 Why Given/When/Then?

1. **Clear behavior specification** - What the code does
2. **Test-driven** - Tests built into spec
3. **Documentation** - Self-documenting code
4. **BDD standard** - Industry best practice

## 🔥 REMEMBER:

- **Given** = Initial state/precondition
- **When** = Action/event
- **Then** = Expected outcome
- **test_cases** = Concrete examples

## ⚠️ DO NOT:

- ❌ Use plain function definitions without behaviors
- ❌ Skip Given/When/Then
- ❌ Forget test_cases
- ❌ Use custom syntax instead of YAML

## ✅ ALWAYS:

- ✅ Start with behaviors
- ✅ Use Given/When/Then
- ✅ Add multiple test_cases
- ✅ Follow honeycomb examples
