#!/usr/bin/env python3
"""
ГЕНЕРАТОР ИМЁН НА ТРИДЕВЯТИЦЕ

Создаёт уникальные имена для:
- Персонажей
- Мест
- Артефактов
- Заклинаний

Каждое имя имеет числовое значение и перевод.
"""

import random
from typing import Tuple, List

# ═══════════════════════════════════════════════════════════════
# АЛФАВИТ
# ═══════════════════════════════════════════════════════════════

АЛФАВИТ = {
    # Медное царство (1-9) — существительные
    'Ⲁ': (1, 'а', 'начало'),
    'Ⲃ': (2, 'б', 'двойственность'),
    'Ⲅ': (3, 'в', 'тройка'),
    'Ⲇ': (4, 'г', 'слово'),
    'Ⲉ': (5, 'д', 'добро'),
    'Ⲋ': (6, 'е', 'бытие'),
    'Ⲍ': (7, 'ж', 'жизнь'),
    'Ⲏ': (8, 'з', 'земля'),
    'Ⲑ': (9, 'и', 'связь'),
    
    # Серебряное царство (10-18) — глаголы
    'Ⲓ': (10, 'к', 'действие'),
    'Ⲕ': (11, 'л', 'люди'),
    'Ⲗ': (12, 'м', 'мысль'),
    'Ⲙ': (13, 'н', 'наше'),
    'Ⲛ': (14, 'о', 'он'),
    'Ⲝ': (15, 'п', 'покой'),
    'Ⲟ': (16, 'р', 'речь'),
    'Ⲡ': (17, 'с', 'слово'),
    'Ⲣ': (18, 'т', 'твёрдость'),
    
    # Золотое царство (19-27) — качества
    'Ⲥ': (19, 'у', 'учение'),
    'Ⲧ': (20, 'ф', 'плод'),
    'Ⲩ': (21, 'х', 'крест'),
    'Ⲫ': (22, 'ц', 'цель'),
    'Ⲭ': (23, 'ч', 'красота'),
    'Ⲯ': (24, 'ш', 'тишина'),
    'Ⲱ': (25, 'щ', 'защита'),
    'Ⳁ': (26, 'ъ', 'сила'),
    'Ⳃ': (27, 'я', 'полнота'),
}

БУКВЫ = list(АЛФАВИТ.keys())
МЕДНЫЕ = БУКВЫ[:9]
СЕРЕБРЯНЫЕ = БУКВЫ[9:18]
ЗОЛОТЫЕ = БУКВЫ[18:]

# ═══════════════════════════════════════════════════════════════
# КОРНИ СЛОВ
# ═══════════════════════════════════════════════════════════════

КОРНИ_ГЕРОЕВ = {
    'ⲀⲄ': 'герой',
    'ⲀⲈ': 'мудрец',
    'ⲀⲊ': 'воин',
    'ⲀⲌ': 'маг',
    'ⲀⲎ': 'странник',
    'ⲀⲐ': 'искатель',
    'ⲂⲀ': 'защитник',
    'ⲂⲄ': 'хранитель',
    'ⲂⲈ': 'целитель',
}

КОРНИ_МЕСТ = {
    'ⲂⲂ': 'дом',
    'ⲂⲄ': 'царство',
    'ⲂⲆ': 'мир',
    'ⲂⲈ': 'путь',
    'ⲂⲊ': 'лес',
    'ⲂⲌ': 'гора',
    'ⲂⲎ': 'река',
    'ⲂⲐ': 'море',
    'ⲄⲀ': 'небо',
}

КОРНИ_АРТЕФАКТОВ = {
    'ⲄⲂ': 'меч',
    'ⲄⲄ': 'щит',
    'ⲄⲆ': 'книга',
    'ⲄⲈ': 'ключ',
    'ⲄⲊ': 'кольцо',
    'ⲄⲌ': 'посох',
    'ⲄⲎ': 'амулет',
    'ⲄⲐ': 'камень',
    'ⲆⲀ': 'огонь',
}

КОРНИ_КАЧЕСТВ = {
    'ⲤⲀ': 'великий',
    'ⲤⲂ': 'малый',
    'ⲤⲄ': 'добрый',
    'ⲤⲆ': 'тёмный',
    'ⲤⲈ': 'мудрый',
    'ⲤⲊ': 'сильный',
    'ⲤⲌ': 'быстрый',
    'ⲤⲎ': 'вечный',
    'ⲤⲐ': 'святой',
}

# ═══════════════════════════════════════════════════════════════
# ФУНКЦИИ
# ═══════════════════════════════════════════════════════════════

def значение(имя: str) -> int:
    """Вычисляет числовое значение имени"""
    return sum(АЛФАВИТ[c][0] for c in имя if c in АЛФАВИТ)

def транслит(имя: str) -> str:
    """Транслитерация на кириллицу"""
    return ''.join(АЛФАВИТ[c][1] for c in имя if c in АЛФАВИТ)

def сгенерировать_имя_героя() -> Tuple[str, str, int]:
    """Генерирует имя героя"""
    # Корень + качество + суффикс
    корень = random.choice(list(КОРНИ_ГЕРОЕВ.keys()))
    качество = random.choice(list(КОРНИ_КАЧЕСТВ.keys()))
    суффикс = random.choice(МЕДНЫЕ[:3])  # Ⲁ, Ⲃ, Ⲅ
    
    имя = корень + качество + суффикс
    перевод = f"{КОРНИ_КАЧЕСТВ[качество]} {КОРНИ_ГЕРОЕВ[корень]}"
    
    return имя, перевод, значение(имя)

def сгенерировать_имя_места() -> Tuple[str, str, int]:
    """Генерирует название места"""
    # Качество + корень
    качество = random.choice(list(КОРНИ_КАЧЕСТВ.keys()))
    корень = random.choice(list(КОРНИ_МЕСТ.keys()))
    
    имя = качество + корень
    перевод = f"{КОРНИ_КАЧЕСТВ[качество]} {КОРНИ_МЕСТ[корень]}"
    
    return имя, перевод, значение(имя)

def сгенерировать_имя_артефакта() -> Tuple[str, str, int]:
    """Генерирует название артефакта"""
    # Корень + качество + "Ⲅ" (тройка)
    корень = random.choice(list(КОРНИ_АРТЕФАКТОВ.keys()))
    качество = random.choice(list(КОРНИ_КАЧЕСТВ.keys()))
    
    имя = корень + качество + 'Ⲅ'
    перевод = f"{КОРНИ_КАЧЕСТВ[качество]} {КОРНИ_АРТЕФАКТОВ[корень]} Тройки"
    
    return имя, перевод, значение(имя)

def сгенерировать_заклинание() -> Tuple[str, str, int]:
    """Генерирует заклинание"""
    # Глагол + объект + сила
    глагол = random.choice(СЕРЕБРЯНЫЕ)
    объект = random.choice(МЕДНЫЕ)
    сила = random.choice(ЗОЛОТЫЕ)
    
    имя = глагол + объект + сила
    
    действия = ['призвать', 'создать', 'разрушить', 'защитить', 'исцелить']
    объекты = ['огонь', 'воду', 'землю', 'воздух', 'свет', 'тьму', 'время']
    силы = ['великой силой', 'мудростью', 'любовью', 'волей', 'верой']
    
    перевод = f"{random.choice(действия)} {random.choice(объекты)} {random.choice(силы)}"
    
    return имя, перевод, значение(имя)

def сгенерировать_имя_по_значению(целевое_значение: int) -> Tuple[str, str, int]:
    """Генерирует имя с заданным числовым значением"""
    # Пробуем найти комбинацию
    for _ in range(1000):
        длина = random.randint(2, 5)
        буквы = random.choices(БУКВЫ, k=длина)
        имя = ''.join(буквы)
        
        if значение(имя) == целевое_значение:
            return имя, f"[имя со значением {целевое_значение}]", целевое_значение
    
    # Если не нашли точное, возвращаем ближайшее
    return сгенерировать_имя_героя()

def сгенерировать_священное_имя() -> Tuple[str, str, int]:
    """Генерирует священное имя (делится на 3, 9 или 27)"""
    while True:
        имя, перевод, знач = сгенерировать_имя_героя()
        if знач % 27 == 0:
            return имя, перевод + " (священное, ÷27)", знач
        elif знач % 9 == 0:
            return имя, перевод + " (благословенное, ÷9)", знач
        elif знач % 3 == 0:
            return имя, перевод + " (благое, ÷3)", знач

# ═══════════════════════════════════════════════════════════════
# ГЕНЕРАТОР ПОЛНОГО ИМЕНИ
# ═══════════════════════════════════════════════════════════════

def сгенерировать_полное_имя() -> dict:
    """Генерирует полное имя с описанием"""
    тип = random.choice(['герой', 'место', 'артефакт', 'заклинание'])
    
    if тип == 'герой':
        имя, перевод, знач = сгенерировать_имя_героя()
    elif тип == 'место':
        имя, перевод, знач = сгенерировать_имя_места()
    elif тип == 'артефакт':
        имя, перевод, знач = сгенерировать_имя_артефакта()
    else:
        имя, перевод, знач = сгенерировать_заклинание()
    
    # Определяем царство по значению
    if знач <= 9:
        царство = "Медное"
    elif знач <= 18:
        царство = "Серебряное"
    else:
        царство = "Золотое"
    
    # Определяем особенность
    особенность = ""
    if знач == 27:
        особенность = "🌟 ТРИДЕВЯТОЕ ИМЯ!"
    elif знач == 999:
        особенность = "👑 ИМЯ КНИГИ!"
    elif знач % 27 == 0:
        особенность = "✨ Священное (÷27)"
    elif знач % 9 == 0:
        особенность = "⭐ Благословенное (÷9)"
    elif знач % 3 == 0:
        особенность = "☆ Благое (÷3)"
    
    return {
        'тридевятица': имя,
        'транслит': транслит(имя),
        'перевод': перевод,
        'тип': тип,
        'значение': знач,
        'царство': царство,
        'особенность': особенность,
    }

# ═══════════════════════════════════════════════════════════════
# MAIN
# ═══════════════════════════════════════════════════════════════

if __name__ == "__main__":
    print("═" * 60)
    print("ГЕНЕРАТОР ИМЁН ТРИДЕВЯТОГО ЦАРСТВА")
    print("═" * 60)
    
    print("\n🦸 ГЕРОИ:")
    print("-" * 60)
    for _ in range(3):
        имя, перевод, знач = сгенерировать_имя_героя()
        print(f"  {имя} ({транслит(имя)}) = {перевод} [{знач}]")
    
    print("\n🏰 МЕСТА:")
    print("-" * 60)
    for _ in range(3):
        имя, перевод, знач = сгенерировать_имя_места()
        print(f"  {имя} ({транслит(имя)}) = {перевод} [{знач}]")
    
    print("\n⚔️ АРТЕФАКТЫ:")
    print("-" * 60)
    for _ in range(3):
        имя, перевод, знач = сгенерировать_имя_артефакта()
        print(f"  {имя} ({транслит(имя)}) = {перевод} [{знач}]")
    
    print("\n✨ ЗАКЛИНАНИЯ:")
    print("-" * 60)
    for _ in range(3):
        имя, перевод, знач = сгенерировать_заклинание()
        print(f"  {имя} ({транслит(имя)}) = {перевод} [{знач}]")
    
    print("\n🌟 СВЯЩЕННЫЕ ИМЕНА (делятся на 3/9/27):")
    print("-" * 60)
    for _ in range(3):
        имя, перевод, знач = сгенерировать_священное_имя()
        print(f"  {имя} = {перевод} [{знач}]")
    
    print("\n📋 ПОЛНЫЕ ИМЕНА:")
    print("-" * 60)
    for _ in range(3):
        данные = сгенерировать_полное_имя()
        print(f"""
  Тридевятица: {данные['тридевятица']}
  Транслит:    {данные['транслит']}
  Перевод:     {данные['перевод']}
  Тип:         {данные['тип']}
  Значение:    {данные['значение']}
  Царство:     {данные['царство']}
  {данные['особенность']}
""")
    
    print("\n" + "═" * 60)
    print("ⲨⲀ ⲒⲈ ⲦⲀ!")
    print("═" * 60)
