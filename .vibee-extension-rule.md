# 🚫 CRITICAL: spec.yml is FORBIDDEN!

## Compiler-Level Enforcement

**VIBEEC compiler ONLY accepts .vibee extension!**

### ❌ FORBIDDEN:
```
spec.yml
spec.yaml
*.yml
*.yaml
```

### ✅ REQUIRED:
```
spec.vibee
```

## Why?

1. **Clear distinction** - .vibee is our language
2. **No confusion** - Not generic YAML
3. **Compiler enforcement** - Automatic validation
4. **Tooling support** - Editors recognize .vibee

## Enforcement

The compiler checks for forbidden files:

```gleam
// In vibeec/src/spec_validator.gleam
pub fn enforce_vibee_format() -> Result(Nil, String) {
  // Finds any spec.yml files
  // Returns ERROR if found
  // Compilation FAILS
}
```

## Error Message

If you try to use spec.yml:

```
╔══════════════════════════════════════════════════════════════╗
║  🚫 ERROR: spec.yml is FORBIDDEN! 🚫                        ║
╚══════════════════════════════════════════════════════════════╝

VIBEEC compiler ONLY accepts .vibee format!

❌ WRONG: spec.yml
✅ CORRECT: spec.vibee

To fix:
  mv spec.yml spec.vibee
```

## Migration

All 127 spec.yml files have been renamed:

```bash
find honeycomb -name "spec.yml" -exec mv {} {}.vibee \;
```

Result:
- honeycomb/telegram/telegram_bot/spec.vibee ✅
- honeycomb/knowledge/knowledge_base/spec.vibee ✅
- honeycomb/tools/language_generator/spec.vibee ✅
- ... (127 total)

## Rules

1. ✅ Extension MUST be .vibee
2. ✅ Syntax MUST be YAML
3. ✅ Structure MUST be Given/When/Then
4. 🚫 spec.yml is FORBIDDEN
5. 🚫 spec.yaml is FORBIDDEN
6. 🚫 *.yml is NOT recognized
7. 🚫 *.yaml is NOT recognized

## Compiler Behavior

```
vibee gen
  ↓
Check for spec.yml files
  ↓
If found → ERROR + EXIT
  ↓
If not found → Continue compilation
```

## Remember

- **Extension**: .vibee (NOT .yml)
- **Enforcement**: Compiler-level
- **No exceptions**: ZERO tolerance
- **Migration**: Complete (127/127)

🚫 **spec.yml is DEAD!**  
✅ **spec.vibee is ALIVE!**
