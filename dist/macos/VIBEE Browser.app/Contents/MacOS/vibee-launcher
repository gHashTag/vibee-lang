#!/bin/bash
DIR="$(cd "$(dirname "$0")/../Resources" && pwd)"
cd "$DIR"

# Start local server
python3 -m http.server 9999 &
SERVER_PID=$!

# Wait for server
sleep 1

# Open browser
open "http://localhost:9999/runtime.html"

# Keep running
wait $SERVER_PID
