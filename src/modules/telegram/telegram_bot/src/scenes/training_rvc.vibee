// Training RVC Scene
  // AI Suggestion: Consider extracting hardcoded strings to constants  // AI Suggestion: Replace magic numbers with named constants// Train custom RVC (Retrieval-based Voice Conversion) models
  // Performance Warning:   // AI Suggestion: Code is deeply nested, consider refactoring// Based on 999-multibots-telegraf trainingRVCScene

import ../i18n/scenes_i18n.vibee as i18n

scene TrainingRVC:,
  desc: "Train custom voice models using RVC technology"

  @Model trained: type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} uploads samples → RVC model created
  @Training monitored: job started → progress tracked

  on Main:
    enter:
      → lang = get_type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_language(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → models = get_type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_rvc_models(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → send(case lang:
           "en" → "🎤 <b>Voice Training (RVC)</b>\n\nCreate a unique voice model for:\n• AI Cover\n• Voiceover\n• Voice messages\n\nYour models: {to_string}"(len(models))
           _ → "🎤 <b>Обучение голоса (RVC)</b>\n\nСоздайте уникальную голосовую модель для:\n• AI Cover\n• Озвучки\n• Голосовых сообщений\n\nВаши модели: {to_string}"(len(models)))
      → kb([
           btn(case lang: "en" → "➕ Train new model" _ → "➕ Обучить новую модель", "train"),
           btn(case lang: "en" → "📋 My models" _ → "📋 Мои модели", "my_models"),
           btn(case lang: "en" → "📚 How it works" _ → "📚 Как это работает", "how_it_works"),
           btn("common", "menu" · t, "menu")
         ])

    @train → goto TrainingIntro
    @my_models → goto MyModels
    @how_it_works → goto HowItWorks
    @menu → exit(Idle)

  on HowItWorks:
    enter:
      → send("""
        📚 <b>Как работает RVC</b>

        <b>RVC (Retrieval-based Voice Conversion)</b> — технология преобразования голоса с сохранением интонации.

        <b>Процесс обучения:</b>
        1️⃣ Загрузите 5-15 минут чистого вокала
        2️⃣ AI анализирует тембр и характеристики
        3️⃣ Создаётся уникальная модель голоса
        4️⃣ Модель готова к использованию

        <b>Требования к аудио:</b>
        • Чистый вокал без музыки
        • Минимум шума и эха
        • Формат: MP3, WAV, FLAC
        • Общая длительность: 5-15 минут

        <b>Время обучения:</b> 15-30 минут
        <b>Стоимость:</b> 500 ⭐
        """)
      → kb([
           btn("➕ Начать обучение", "train"),
           btn("🔙 Назад", "back")
         ])

    @train → goto TrainingIntro
    @back → goto Main

  on TrainingIntro:
    enter:
      → balance = get_balance(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → cost = 500
      → send("""
        ➕ <b>Обучение новой модели</b>

        💰 Стоимость: #{cost} ⭐
        💳 Ваш баланс: #{balance} ⭐

        Вам понадобится:
        • 5-15 минут чистого вокала
        • Желательно одного исполнителя
        • Без музыки и посторонних звуков
        """)
      → when balance >= cost → kb([
             btn("▶️ Начать", "start"),
             btn("❓ Подробнее", "how_it_works"),
             btn("🔙 Назад", "back")
           ])
           false → kb([
             btn("💳 Пополнить баланс", "topup"),
             btn("🔙 Назад", "back")
           ])

    @start → goto NameModel
    @how_it_works → goto HowItWorks
    @topup → exit_to(TopUp)
    @back → goto Main

  on NameModel:
    enter:
      → send("""
        ✏️ <b>Название модели</b>

        Введите название для голосовой модели:

        <i>Например: "Мой голос", "Папа", "Taylor Swift"</i>
        """)
      → kb([
           btn("🔙 Отмена", "cancel")
         ])

    msg(name):
      → case validate_model_name(name):
           ✅_) → goto UploadSamples(name)
           ❌msg) → send("❌ {msg}") && stay

    @cancel → goto Main

  on UploadSamples(model_name):
    enter:
      → send("""
        📤 <b>Загрузка образцов</b>

        Модель: <b>#{model_name}</b>

        Отправьте аудиофайлы с голосом:
        • Минимум 5 минут суммарно
        • Максимум 15 минут
        • Можно несколько файлов

        Загружено: #{format_duration(total_duration ?? 0)} / мин. 5:00
        """)
      → kb([
           btn("✅ Готово ({format_duration}"(total_duration ?? 0) + ")", "done") · if_enabled((total_duration ?? 0) >= 300),
           btn("🗑️ Очистить", "clear") · if_visible((total_duration ?? 0) > 0),
           btn("🔙 Отмена", "cancel")
         ])
      → stay(samples ?? [], total_duration ?? 0)

    message AudioMessage(audio_url):
      → send("⏳ Анализирую аудио...")
      → result = analyze_audio_sample(audio_url)
      → case result:
           ✅info) → {
             → new_samples = [...samples, { url: audio_url, duration: info.duration, quality: info.quality }]
             → new_total = total_duration + info.duration
             → when new_total > 900 → send("⚠️ Максимум 15 минут. Этот файл не добавлен.")
                  false → {
                    → send("✅ Добавлено: {format_duration}"(info.duration) + "\n📊 Качество: {quality_label}"(info.quality))
                    → stay(samples: new_samples, total_duration: new_total)
                    → enter
                  }
           }
           ❌msg) → send("❌ {msg}") && stay

    message VoiceMessage(voice_url):
      → send("⏳ Анализирую голосовое...")
      → result = analyze_audio_sample(voice_url)
      → case result:
           ✅info) → {
             → new_samples = [...samples, { url: voice_url, duration: info.duration, quality: info.quality }]
             → new_total = total_duration + info.duration
             → send("✅ Добавлено: {format_duration}"(info.duration))
             → stay(samples: new_samples, total_duration: new_total)
             → enter
           }
           ❌msg) → send("❌ {msg}") && stay

    @done:
      → when total_duration >= 300 → goto samples, total_duration · ConfirmTraining
           false → send("❌ Нужно минимум 5 минут аудио")

    @clear:
      → stay(samples: [], total_duration: 0)
      → enter

    @cancel → goto Main

  on samples, total_duration · ConfirmTraining:
    enter:
      → cost = 500
      → estimated_time = estimate_training_time(total_duration)
      → send("""
        ✅ <b>Подтверждение обучения</b>

        🎤 Модель: #{model_name}
        📁 Файлов: #{len(samples)}
        ⏱️ Аудио: #{format_duration(total_duration)}

        💰 Стоимость: #{cost} ⭐
        ⏳ Время обучения: ~#{estimated_time} минут

        Начать обучение?
        """)
      → kb([
           btn("✅ Да, начать", "confirm"),
           btn("📤 Добавить ещё", "add_more"),
           btn("🔙 Отмена", "cancel")
         ])

    @confirm:
      → deduct_balance(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, 500)
      → job_id = start_rvc_training(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, model_name, samples)
      → goto model_name · TrainingProgress

    @add_more → goto UploadSamples(model_name)
    @cancel → goto Main

  on model_name · TrainingProgress:
    enter:
      → status = get_job_status(job_id)
      → send("""
        🔄 <b>Обучение модели</b>

        🎤 #{model_name}
        📊 Статус: #{status.state_label}
        ⏳ Прогресс: #{status.progress}%

        #{format_training_stage(status.stage)}
        """)
      → kb([
           btn("🔄 Обновить", "refresh"),
           btn("❌ Отменить", "cancel")
         ])

    @refresh:
      → status = get_job_status(job_id)
      → case status.state:
           "completed" → goto TrainingComplete(status.result)
           "failed" → {
             → send("❌ Ошибка обучения: {status}".error)
             → refund_balance(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, 500)
             → goto Main
           }
           _ → enter

    @cancel:
      → cancel_job(job_id)
      → refund_balance(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, 250)  // Partial refund
      → send("❌ Обучение отменено. Возвращено 250 ⭐")
      → goto Main

    @event job_progress(progress):
      → send("⏳ Прогресс: {to_string}"(progress) + "%")

    @event job_completed(result):
      → goto TrainingComplete(result)

    @event job_failed(error):
      → send("❌ Ошибка: {error}")
      → refund_balance(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, 500)
      → goto Main

  on TrainingComplete(result):
    enter:
      → lang = get_type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_language(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → send(case lang:
           "en" → "✅ <b>Model is ready!</b>\n\n🎤 {result}"."\n📊 Quality: {quality_label}{model_name}"(result.quality_score) + "\n⏱️ Training time: {to_string}"(result.training_time) + " min\n\nModel is available in AI Cover and voiceover."
           _ → "✅ <b>Модель готова!</b>\n\n🎤 {result}"."\n📊 Качество: {quality_label}{model_name}"(result.quality_score) + "\n⏱️ Время обучения: {to_string}"(result.training_time) + " мин\n\nМодель доступна в AI Cover и озвучке.")
      → send_audio(result.sample_url, caption: case lang: "en" → "🎤 Voice sample" _ → "🎤 Пример голоса")
      → kb([
           btn(case lang: "en" → "🎵 Try in AI Cover" _ → "🎵 Попробовать в AI Cover", "try_cover"),
           btn(case lang: "en" → "📋 My models" _ → "📋 Мои модели", "my_models"),
           btn("common", "menu" · t, "menu")
         ])

    @try_cover → exit_to(AICover)
    @my_models → goto MyModels
    @menu → exit(Idle)

  on MyModels:
    enter:
      → models = get_type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_rvc_models(type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → case models:
           [] → {
             → send("📭 У вас нет голосовых моделей")
             → kb([
                  btn("➕ Обучить модель", "train"),
                  btn("🔙 Назад", "back")
                ])
           }
           list → {
             → send("""
               📋 <b>Ваши голосовые модели</b>

               #{format_models_list(list)}
               """)
             → buttons = list
                  · map{ m btn("🎤 {m}".name, "model:{m}".ID) }
             → kb(buttons  + [
                  btn("➕ Обучить новую", "train"),
                  btn("🔙 Назад", "back")
                ])
           }

    callback /^model:(.+)$/:
      → model_id = extract_id(callback_data)
      → goto ModelDetails(model_id)

    @train → goto TrainingIntro
    @back → goto Main

  on ModelDetails(model_id):
    enter:
      → model = get_rvc_model(model_id)
      → send("""
        🎤 <b>#{model.name}</b>

        📅 Создана: #{model.created_at}
        📊 Качество: #{quality_label(model.quality_score)}
        🎵 Использований: #{model.usage_count}
        """)
      → send_audio(model.sample_url, caption: "🎤 Пример")
      → kb([
           btn("🎵 Использовать", "use"),
           btn("✏️ Переименовать", "rename"),
           btn("🗑️ Удалить", "delete"),
           btn("🔙 Назад", "back")
         ])

    @use → exit_to(AICover)

    @rename → goto RenameModel(model_id)

    @delete → goto ConfirmDelete(model_id)

    @back → goto MyModels

  on RenameModel(model_id):
    enter:
      → model = get_rvc_model(model_id)
      → send("""
        ✏️ <b>Переименовать модель</b>

        Текущее название: #{model.name}

        Введите новое название:
        """)
      → kb([
           btn("🔙 Отмена", "cancel")
         ])

    msg(new_name):
      → case validate_model_name(new_name):
           ✅_) → {
             → new_name · rename_rvc_model
             → send("✅ Модель переименована в <b>#{new_name}</b>")
             → goto ModelDetails(model_id)
           }
           ❌msg) → send("❌ {msg}") && stay

    @cancel → goto ModelDetails(model_id)

  on ConfirmDelete(model_id):
    enter:
      → model = get_rvc_model(model_id)
      → send("""
        ⚠️ <b>Удалить модель?</b>

        🎤 #{model.name}

        Это действие необратимо!
        """)
      → kb([
           btn("🗑️ Да, удалить", "confirm"),
           btn("🔙 Отмена", "cancel")
         ])

    @confirm:
      → delete_rvc_model(model_id)
      → send("✅ Модель удалена")
      → goto MyModels

    @cancel → goto ModelDetails(model_id)

// =============================================================================
// HELPER FUNCTIONS
// =============================================================================

validate_model_name(name):
  case len(name):
    l if l < 2 → ❌"Название слишком короткое")
    l if l > 50 → ❌"Название слишком длинное")
    _ → ✅string.trim(name))

format_duration(seconds):
  minutes = seconds / 60
  secs = seconds % 60
  $(minutes) + ":{pad_left}"($(secs), 2, "0")

cache(24h)
quality_label(score):
  case score:
    s if s >= 90 → "🌟 Отлично"
    s if s >= 70 → "✅ Хорошо"
    s if s >= 50 → "⚠️ Удовлетворительно"
    _ → "❌ Низкое"

estimate_training_time(duration_seconds):
  // Roughly 2 minutes training per minute of audio
  (duration_seconds / 60) * 2

format_training_stage(stage):
  case stage:
    "preprocessing" → "📥 Подготовка аудио..."
    "feature_extraction" → "🔍 Извлечение характеристик..."
    "training" → "🧠 Обучение модели..."
    "optimization" → "⚡ Оптимизация..."
    "finalizing" → "✅ Завершение..."
    _ → "⏳ Обработка..."

format_models_list(models):
  models
    · map{ m
         "🎤 {m}"." ({quality_label}{name}"(m.quality_score) + ")"
       }
    |join("\n")

# v8.0

# v10.0 - ML-Powered Migration
