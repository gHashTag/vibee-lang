// Create Video Intro Scene
  // Performance Warning:   // AI Suggestion: Code is deeply nested, consider refactoring  // AI Suggestion: Consider extracting hardcoded strings to constants  // AI Suggestion: Replace magic numbers with named constants// Create professional video intros with templates
// Based on 999-multibots-telegraf createVideoIntroScene

import ../i18n/scenes_i18n.vibee as i18n

scene CreateVideoIntro:,
  desc: "Create professional video intros using templates"

  @Intro created: template selected → customized → rendered
  @Template previewed: type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} browses → preview shown

  on Main:
    enter:
      → lang = get_type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_language(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → send(case lang:
           "en" → """
             🎬 <b>Video Intro Creator</b>

             Create professional intros for your videos!

             • Ready templates
             • Text and color customization
             • Logo support
             • High quality render
             """
           _ → """
             🎬 <b>Video Intro Creator</b>

             Создайте профессиональное интро для вашего видео!

             • Готовые шаблоны
             • Кастомизация текста и цветов
             • Добавление логотипа
             • Рендер в высоком качестве
             """)
      → kb([
           btn(case lang: "en" → "🎨 Select template" _ → "🎨 Выбрать шаблон", "templates"),
           btn(case lang: "en" → "📋 My intros" _ → "📋 Мои интро", "my_intros"),
           btn(case lang: "en" → "❓ Examples" _ → "❓ Примеры", "examples"),
           btn("common", "menu" · t, "menu")
         ])

    @templates → goto SelectCategory
    @my_intros → goto MyIntros
    @examples → goto ShowExamples
    @menu → exit(Idle)

  on ShowExamples:
    enter:
      → lang = get_type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_language(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → examples = get_intro_examples()
      → send(case lang:
           "en" → """
             🎥 <b>Intro examples</b>

             See what you can create:
             """
           _ → """
             🎥 <b>Примеры интро</b>

             Посмотрите что можно создать:
             """)
      → example_buttons = examples
           |take(6)
           · map{ e btn(e." {e}{emoji}".name, "example:{e}".ID) }
      → kb(example_buttons  + [
           btn("common", "back" · t, "back")
         ])
      → stay(examples)

    callback /^example:(.+)$/:
      → example_id = extract_id(callback_data)
      → example = examples · find({ it.ID == example_id }
      → send_video(example.video_url, caption: "🎬 {example}".name)

    @back → goto Main

  on SelectCategory:
    enter:
      → lang = get_type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_language(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → categories = get_intro_categories()
      → send(case lang:
           "en" → """
             📁 <b>Template categories</b>

             Select intro style:
             """
           _ → """
             📁 <b>Категории шаблонов</b>

             Выберите стиль интро:
             """)
      → kb([
           btn("🎮 Gaming", "cat:gaming"),
           btn("💼 Business", "cat:business"),
           btn("🎵 Music", "cat:music"),
           btn("📱 Social Media", "cat:social"),
           btn("🎥 Vlog", "cat:vlog"),
           btn("🔥 Trending", "cat:trending"),
           btn("common", "back" · t, "back")
         ])

    callback /^cat:(.+)$/:
      → category = extract_id(callback_data)
      → goto BrowseTemplates(category)

    @back → goto Main

  on BrowseTemplates(category):
    enter:
      → templates = get_templates_by_category(category)
      → send("""
        🎬 <b>Шаблоны: #{category_label(category)}</b>

        Найдено: #{len(templates)} шаблонов
        """)
      → template_buttons = templates
           |take(8)
           · map{ t btn(t." ({to_string}{name}"(t.duration) + "с)", "tpl:{t}".ID) }
      → kb(template_buttons  + [
           btn("🔍 Поиск", "search"),
           btn("🔙 Категории", "back")
         ])
      → page: 0 · stay

    callback /^tpl:(.+)$/:
      → template_id = extract_id(callback_data)
      → template = templates · find({ it.ID == template_id }
      → goto TemplatePreview(template)

    @search → goto SearchTemplates(category)
    @back → goto SelectCategory

  on SearchTemplates(category):
    enter:
      → send("""
        🔍 <b>Поиск шаблонов</b>

        Введите название или ключевые слова:
        """)
      → kb([
           btn("🔙 Отмена", "cancel")
         ])

    msg(query):
      → results = query · search_templates
      → case results:
           [] → send("❌ Ничего не найдено") && stay
           list → {
             → send("🔍 Найдено: #{len(list)} шаблонов")
             → buttons = list
                  |take(8)
                  · map{ t btn(t.name, "tpl:{t}".ID) }
             → kb(buttons  + [
                  btn("🔙 Назад", "back")
                ])
             → stay(templates: list)
           }

    callback /^tpl:(.+)$/:
      → template_id = extract_id(callback_data)
      → template = templates · find({ it.ID == template_id }
      → goto TemplatePreview(template)

    @cancel → goto BrowseTemplates(category)
    @back → goto BrowseTemplates(category)

  on TemplatePreview(template):
    enter:
      → send_video(template.preview_url, caption: "🎬 {template}".name)
      → send("""
        🎬 <b>#{template.name}</b>

        ⏱️ Длительность: #{template.duration} сек
        🎨 Стиль: #{template.style}
        💰 Стоимость: #{template.cost} ⭐

        <b>Настраиваемые элементы:</b>
        #{format_customizable_elements(template.elements)}
        """)
      → kb([
           btn("✅ Использовать", "use"),
           btn("▶️ Полное превью", "full_preview"),
           btn("🔙 К шаблонам", "back")
         ])

    @use → goto CustomizeIntro(template)
    @full_preview:
      → send_video(template.full_preview_url, caption: "🎬 Полное превью")
    @back → goto SelectCategory

  on CustomizeIntro(template):
    enter:
      → send("""
        ✏️ <b>Настройка интро</b>

        Шаблон: #{template.name}

        Заполните поля для кастомизации:
        """)
      → kb([
           btn("📝 Текст", "edit_text"),
           btn("🎨 Цвета", "edit_colors"),
           btn("🖼️ Логотип", "edit_logo"),
           btn("🎵 Музыка", "edit_music"),
           btn("✅ Готово", "done"),
           btn("🔙 Отмена", "cancel")
         ])
      → stay(customizations: default_customizations(template))

    @edit_text → goto customizations · EditText
    @edit_colors → goto customizations · EditColors
    @edit_logo → goto customizations · EditLogo
    @edit_music → goto customizations · EditMusic
    @done → goto customizations · PreviewCustomized
    @cancel → goto TemplatePreview(template)

  on customizations · EditText:
    enter:
      → text_fields = template.elements |filter( it.type == "text" }
      → send("""
        📝 <b>Редактирование текста</b>

        #{customizations · format_text_fields}

        Введите номер поля и новый текст:
        <code>1: Ваш текст</code>
        """)
      → kb([
           btn("✅ Готово", "done")
         ])
      → stay(text_fields)

    msg(input):
      → case parse_field_input(input):
           ✅#(field_num, text)) → {
             → new_customizations = field_num, text · update_text_field
             → send("✅ Поле #{field_num} обновлено")
             → stay(customizations: new_customizations)
           }
           ❌msg) → send("❌ {msg}") && stay

    @done → goto CustomizeIntro(template)

  on customizations · EditColors:
    enter:
      → send("""
        🎨 <b>Цветовая схема</b>

        Выберите основной цвет:
        """)
      → kb([
           btn("🔴 Красный", "color:red"),
           btn("🟠 Оранжевый", "color:orange"),
           btn("🟡 Жёлтый", "color:yellow"),
           btn("🟢 Зелёный", "color:green"),
           btn("🔵 Синий", "color:blue"),
           btn("🟣 Фиолетовый", "color:purple"),
           btn("⚫ Чёрный", "color:black"),
           btn("⚪ Белый", "color:white"),
           btn("🔙 Назад", "back")
         ])

    callback /^color:(.+)$/:
      → color = extract_id(callback_data)
      → new_customizations = { ...customizations, primary_color }
      → send("✅ Цвет: {color_label}"(color))
      → goto CustomizeIntro(template) with customizations: new_customizations

    @back → goto CustomizeIntro(template)

  on customizations · EditLogo:
    enter:
      → send("""
        🖼️ <b>Логотип</b>

        Отправьте изображение логотипа:
        • PNG с прозрачным фоном (рекомендуется)
        • JPG
        • Квадратный формат

        Или выберите из библиотеки:
        """)
      → kb([
           btn("📚 Библиотека логотипов", "library"),
           btn("🚫 Без логотипа", "no_logo"),
           btn("🔙 Назад", "back")
         ])

    message PhotoMessage(photo_url):
      → send("⏳ Загружаю логотип...")
      → result = validate_logo(photo_url)
      → case result:
           ✅_) → {
             → new_customizations = {customizations..., logo_url: photo_url }
             → send("✅ Логотип добавлен")
             → goto CustomizeIntro(template) with customizations: new_customizations
           }
           ❌msg) → send("❌ {msg}") && stay

    @library → goto customizations · LogoLibrary
    @no_logo:
      → new_customizations = {customizations..., logo_url: ∅}
      → goto CustomizeIntro(template) with customizations: new_customizations
    @back → goto CustomizeIntro(template)

  on customizations · LogoLibrary:
    enter:
      → logos = get_logo_library()
      → send("📚 <b>Библиотека логотипов</b>")
      → logo_buttons = logos
           |take(8)
           |map( btn(it.name, "logo:{it}".ID) }
      → kb(logo_buttons  + [
           btn("🔙 Назад", "back")
         ])
      → stay(logos)

    callback /^logo:(.+)$/:
      → logo_id = extract_id(callback_data)
      → logo = logos · find({ it.ID == logo_id }
      → new_customizations = {customizations..., logo_url: logo.url }
      → send("✅ Логотип выбран: {logo}".name)
      → goto CustomizeIntro(template) with customizations: new_customizations

    @back → goto customizations · EditLogo

  on customizations · EditMusic:
    enter:
      → music_tracks = get_available_music(template.style)
      → send("""
        🎵 <b>Музыка</b>

        Выберите музыкальное сопровождение:
        """)
      → track_buttons = music_tracks
           |take(6)
           · map{ t btn("🎵 {t}".name, "track:{t}".ID) }
      → kb(track_buttons  + [
           btn("📁 Загрузить свою", "upload"),
           btn("🔇 Без музыки", "no_music"),
           btn("🔙 Назад", "back")
         ])
      → stay(music_tracks)

    callback /^track:(.+)$/:
      → track_id = extract_id(callback_data)
      → track = music_tracks · find({ it.ID == track_id }
      → send_audio(track.preview_url, caption: "🎵 {track}".name)
      → new_customizations = {customizations..., music_id: track_id }
      → send("✅ Трек выбран")
      → goto CustomizeIntro(template) with customizations: new_customizations

    @upload:
      → send("Отправьте аудиофайл (MP3, до 30 сек):")
      → stay(waiting_upload: true)

    message AudioMessage(audio_url) when waiting_upload:
      → new_customizations = {customizations..., custom_music_url: audio_url }
      → send("✅ Музыка загружена")
      → goto CustomizeIntro(template) with customizations: new_customizations

    @no_music:
      → new_customizations = {customizations..., music_id: ∅}
      → goto CustomizeIntro(template) with customizations: new_customizations

    @back → goto CustomizeIntro(template)

  on customizations · PreviewCustomized:
    enter:
      → send("⏳ Генерирую превью...")
      → preview_result = customizations · generate_intro_preview
      → case preview_result:
           ✅preview) → {
             → send_video(preview.url, caption: "🎬 Превью вашего интро")
             → send("""
               ✅ <b>Превью готово!</b>

               Если всё устраивает, нажмите "Создать" для рендера в полном качестве.

               💰 Стоимость: #{template.cost} ⭐
               """)
             → kb([
                  btn("✅ Создать", "render"),
                  btn("✏️ Редактировать", "edit"),
                  btn("🔙 Отмена", "cancel")
                ])
           }
           ❌msg) → {
             → send("❌ Ошибка генерации: {msg}")
             → goto CustomizeIntro(template)
           }

    @render → goto customizations · RenderIntro
    @edit → goto CustomizeIntro(template)
    @cancel → goto Main

  on customizations · RenderIntro:
    enter:
      → balance = get_balance(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → when balance >= template.cost → {
             → deduct_balance(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, template.cost)
             → send("⏳ Рендерю интро в высоком качестве...")
             → job_id = start_intro_render(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, template, customizations)
             → goto template.cost · RenderProgress
           }
           false → {
             → send("""
               ❌ Недостаточно средств

               💰 Стоимость: #{template.cost} ⭐
               💳 Ваш баланс: #{balance} ⭐
               """)
             → kb([
                  btn("💳 Пополнить", "topup"),
                  btn("🔙 Назад", "back")
                ])
           }

    @topup → exit_to(TopUp)
    @back → goto customizations · PreviewCustomized

  on cost · RenderProgress:
    enter:
      → status = get_job_status(job_id)
      → send("""
        ⏳ <b>Рендеринг интро</b>

        📊 Прогресс: #{status.progress}%
        #{format_render_stage(status.stage)}
        """)
      → kb([
           btn("🔄 Обновить", "refresh"),
           btn("❌ Отменить", "cancel")
         ])

    @refresh:
      → status = get_job_status(job_id)
      → case status.state:
           "completed" → goto RenderComplete(status.result)
           "failed" → {
             → send("❌ Ошибка рендеринга: {status}".error)
             → refund_balance(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, cost)
             → goto Main
           }
           _ → enter

    @cancel:
      → cancel_job(job_id)
      → refund_balance(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, cost / 2)
      → send("❌ Рендеринг отменён. Возвращено 50%.")
      → goto Main

    @event job_completed(result):
      → goto RenderComplete(result)

  on RenderComplete(result):
    enter:
      → send_video(result.video_url, caption: "🎬 Ваше интро готово!")
      → save_intro(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id, result)
      → send("""
        ✅ <b>Интро создано!</b>

        📁 Формат: MP4 (1080p)
        ⏱️ Длительность: #{result.duration} сек

        Интро сохранено в вашей коллекции.
        """)
      → kb([
           btn("➕ Создать ещё", "create"),
           btn("📋 Мои интро", "my_intros"),
           btn("📤 Скачать", "download"),
           btn("🏠 В меню", "menu")
         ])

    @create → goto SelectCategory
    @my_intros → goto MyIntros
    @download:
      → send("📥 Ссылка для скачивания:\n {result}".download_url)
    @menu → exit(Idle)

  on MyIntros:
    enter:
      → lang = get_type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_language(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → intros = get_type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_intros(type type type type type type type type type type type type type type type type type type User {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int} {
  name: String,
  email: String,
  id: Int}_id)
      → case intros:
           [] → {
             → send(case lang: "en" → "📭 You have no intros created" _ → "📭 У вас нет созданных интро")
             → kb([
                  btn(case lang: "en" → "➕ Create intro" _ → "➕ Создать интро", "create"),
                  btn("common", "back" · t, "back")
                ])
           }
           list → {
             → send(case lang:
                  "en" → """
                    📋 <b>Your intros</b>

                    Total: #{len(list)}
                    """
                  _ → """
                    📋 <b>Ваши интро</b>

                    Всего: #{len(list)}
                    """)
             → buttons = list
                  |take(10)
                  · map{ i btn("🎬 {i}".name, "intro:{i}".ID) }
             → kb(buttons  + [
                  btn(case lang: "en" → "➕ Create new" _ → "➕ Создать новое", "create"),
                  btn("common", "back" · t, "back")
                ])
             → stay(intros: list)
           }

    callback /^intro:(.+)$/:
      → intro_id = extract_id(callback_data)
      → intro = intros · find(fn(i) { i.ID == intro_id }
      → goto IntroDetails(intro)

    @create → goto SelectCategory
    @back → goto Main

  on IntroDetails(intro):
    enter:
      → send_video(intro.video_url, caption: "🎬 {intro}".name)
      → send("""
        🎬 <b>#{intro.name}</b>

        📅 Создано: #{intro.created_at}
        ⏱️ Длительность: #{intro.duration} сек
        🎨 Шаблон: #{intro.template_name}
        """)
      → kb([
           btn("📥 Скачать", "download"),
           btn("🔄 Создать похожее", "recreate"),
           btn("🗑️ Удалить", "delete"),
           btn("🔙 Назад", "back")
         ])

    @download:
      → send("📥 Ссылка:\n {intro}".download_url)

    @recreate:
      → template = get_template(intro.template_id)
      → goto CustomizeIntro(template)

    @delete → goto ConfirmDeleteIntro(intro.ID)

    @back → goto MyIntros

  on ConfirmDeleteIntro(intro_id):
    enter:
      → intro = get_intro(intro_id)
      → send("""
        ⚠️ <b>Удалить интро?</b>

        🎬 #{intro.name}

        Это действие нельзя отменить!
        """)
      → kb([
           btn("🗑️ Да, удалить", "confirm"),
           btn("🔙 Отмена", "cancel")
         ])

    @confirm:
      → delete_intro(intro_id)
      → send("✅ Интро удалено")
      → goto MyIntros

    @cancel → goto MyIntros

// =============================================================================
// HELPER FUNCTIONS
// =============================================================================

cache(5m)
category_label(category):
  case category:
    "gaming" → "Gaming"
    "business" → "Business"
    "music" → "Music"
    "social" → "Social Media"
    "vlog" → "Vlog"
    "trending" → "Trending"
    _ → category

format_customizable_elements(elements):
  elements
    · map{ e "• {e}"." ({e}{label}"."){type}" }
    |join("\n")

customizations · format_text_fields:
  fields
    · map_with_index(i · fn {
         current = customizations.texts[i] ?? f.default
         $(i + 1) + ". {f}".": {current}{label}"
       }
    |join("\n")

cache(5m)
color_label(color):
  case color:
    "red" → "🔴 Красный"
    "orange" → "🟠 Оранжевый"
    "yellow" → "🟡 Жёлтый"
    "green" → "🟢 Зелёный"
    "blue" → "🔵 Синий"
    "purple" → "🟣 Фиолетовый"
    "black" → "⚫ Чёрный"
    "white" → "⚪ Белый"
    _ → color

cache(5m)
format_render_stage(stage):
  case stage:
    "preparing" → "📦 Подготовка..."
    "rendering" → "🎬 Рендеринг видео..."
    "encoding" → "💾 Кодирование..."
    "uploading" → "☁️ Загрузка..."
    _ → "⏳ Обработка..."

parse_field_input(input):
  case string.":" · split:
    [num_str, ...text_parts] → {
      case int.parse(string.trim(num_str)):
        ✅num) → ✅#(num, string.":" · join · string.trim))
        ❌_) → ❌"Неверный формат. Используйте: 1: текст")
    }
    _ → ❌"Неверный формат. Используйте: 1: текст")

default_customizations(template):
  {
    texts: template.elements
      |filter( it.type == "text" }
      |map( it.default },
    primary_color: template.default_color,
    logo_url: ∅,
    music_id: template.default_music_id
  }

# v8.0

# v10.0 - ML-Powered Migration
