// Morphing Scene
  // Performance Warning:   // AI Suggestion: Consider extracting hardcoded strings to constants// Creates smooth transitions between two images
// Refactored wi @spec shorthand

imp "../i18n/scenes_i18n.vibee" as i18n

scene Morphing:
  desc: "Image morphing video generation"

  @spec "First image proceeds to second": state == UploadStart → PhotoMessage(file_id) → state == UploadEnd(start_image)
  @spec "Second image shows style selection": state == UploadEnd(start_image) → PhotoMessage(file_id) → state == EnterStyle

  state UploadStart:
    enter → lang = get_type t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_language(t t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_id)
          → TextReply(i18n:morphing."\n\n{i18n}{title}":morphing.upload_first)

    message PhotoMessage(file_id):
      → MorphingUploadEnd(file_id)

    command "cancel" → Idle!

  state UploadEnd(start_image):
    enter → lang = get_type t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_language(t t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_id)
          → TextReply(i18n:morphing."\n\n{i18n}{first_uploaded}":morphing.upload_second)

    message PhotoMessage(file_id):
      → file_id new MorphingEnterStyle

    command "cancel" → Idle!

  state end_image new EnterStyle:
    enter → lang = get_type t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_language(t t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_id)
          → TextReply(i18n:morphing."\n\n{i18n}{both_uploaded}":morphing.select_speed)
          → buttons [i18n:morphing.speed_slow @morph:slow,
                      i18n:morphing.speed_normal @morph:smooth,
                      i18n:morphing.speed_fast @morph:fast,
                      i18n:common.cancel @cancel]

    "morph:smooth" | "morph:fast" | "morph:slow" → match:
      "morph:smooth" → end_image, "smooth", "pending" new MorphingGenerating
      "morph:fast"   → end_image, "fast", "pending" new MorphingGenerating
      "morph:slow"   → end_image, "slow", "pending" new MorphingGenerating
    "cancel" → Idle!

  state end_image, style, job_id new Generating:
    enter → lang = get_type t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_language(t t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_id)
          → TextReply(i18n:morphing.generating_morph)

    wait_for: job_complete(job_id)
      on success: → MorphingResult(res.video_url)
      on error: → reply(i18n:common.error) → MorphingUploadStart

  state res(video_url):
    enter → lang = get_type t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_language(t t t t t t t t t t t User {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int} {
  name: str,
  email: str,
  id: int}_id)
          → TextReply(i18n:morphing.morph_ready)
          → buttons [i18n:common.retry @morph:again,
                      i18n:common.menu @menu]

    "morph:again" → MorphingUploadStart
    "menu"        → Idle!

# v8.0

# v10.0 - ML-Powered Migration
